{
  "name": "agroguard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n be,fe -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "node scripts/start-backend.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
